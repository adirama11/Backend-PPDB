const createData = require('./create');
const updateData = require('./update');
const dataId     = require('./getId');
const allData = require('./getAll');
const delData     = require('./delete');

module.exports = { createData, updateData, dataId, allData, delData };