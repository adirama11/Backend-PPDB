const createProfil = require('./create');
const allData = require('./getAll');
const dataId = require('./getId');
const updateData = require('./update')

module.exports = { createProfil, allData, dataId, updateData };